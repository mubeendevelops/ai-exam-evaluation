/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness probe — no credential, no database
         * @description `{"status": "ok"}`, always, as long as the process is serving.
         */
        get: operations["health_health_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Exchange an email and password for an access + refresh token
         * @description Verifies a password and starts a session.
         *
         *     THE ONLY UNAUTHENTICATED ENDPOINT THAT TOUCHES THE DATABASE. Everything
         *     it can reach is one row wide (migration 017 §5), it is rate-limited per
         *     IP and per address, and it answers every failure identically.
         */
        post: operations["login_api_v1_auth_login_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/refresh": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Exchange a refresh token for a new access + refresh token
         * @description Rotates the session.
         *
         *     THE PRESENTED TOKEN IS REVOKED IN THE SAME TRANSACTION that issues its
         *     replacement. Rotation is what makes a stolen refresh token a bounded
         *     problem: the thief and the legitimate client cannot both keep using it,
         *     so the theft surfaces as one of them being logged out instead of as a
         *     permanent silent second session.
         *
         *     THE ACCOUNT IS RE-READ, not copied forward from the old token's claims.
         *     Role, college and is_active can all change between login and refresh; a
         *     deactivated or demoted account must stop minting usable access tokens at
         *     that moment, not whenever its refresh token happens to expire. That read
         *     is `auth_user_by_id()` — migration 017 §5, which returns no hash.
         */
        post: operations["refresh_api_v1_auth_refresh_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revoke a refresh token (and optionally every other session)
         * @description Ends the session the refresh token represents.
         *
         *     ALWAYS 204, whether the token was live, already revoked, or never
         *     existed. Reporting the difference would turn logout into an oracle over
         *     tokens the caller does not hold, and there is nothing a client could
         *     usefully do with the distinction anyway — either way, that token is now
         *     dead.
         *
         *     WHAT LOGOUT CANNOT DO: kill the access token. It is a signed bearer
         *     credential with no server-side row, so it stays valid until it expires
         *     (15 minutes by default). That is the trade named in api/deps/identity.py,
         *     and it is why the access TTL is short. A client logging out should also
         *     discard its copy.
         *
         *     `all_sessions` requires the presented token to be LIVE — otherwise anyone
         *     holding a revoked or guessed token could sign an account out everywhere.
         */
        post: operations["logout_api_v1_auth_logout_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The account behind the presented access token
         * @description Who the API thinks you are.
         *
         *     TAKES NO DATABASE CONNECTION. Every field here is a signature-verified
         *     claim that `get_current_user` has already checked for internal
         *     consistency, so a query would only re-fetch what the token already says —
         *     and would put a database round trip on the endpoint a client calls on
         *     every page load to decide what to render.
         *
         *     The consequence, stated rather than discovered: this reflects the account
         *     AS OF the access token's issue, so a role changed a minute ago shows here
         *     for up to one access-token lifetime. Requests are authorized from the same
         *     claims, so the answer is at least honest about what the caller can
         *     currently do — and /auth/refresh re-reads the row, which is where a
         *     changed role takes effect.
         */
        get: operations["me_api_v1_auth_me_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/uploads": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List and filter this college's uploaded booklets
         * @description This college's uploaded booklets, newest first — the "unbound
         *     uploads" review queue is `?bound=false`.
         */
        get: operations["list_uploads_api_v1_uploads_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/upload": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Upload a scanned answer booklet (PDF)
         * @description Stores the booklet and records it. Returns 201 with the upload_id.
         *
         *     The tenant comes from the authenticated user via get_tenant_conn — never
         *     from the request — so a caller cannot upload into another college by any
         *     means available to them.
         */
        post: operations["upload_booklet_api_v1_upload_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/jobs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List and filter this college's jobs
         * @description The job dashboard: this college's queue, newest first.
         *
         *     Tenant-scoped exactly like GET /jobs/{job_id} — RLS plus the explicit
         *     college_id predicate in core/jobs.py::list_jobs, for the reasons that
         *     module's header and api/deps/db.py's argue at length.
         */
        get: operations["list_jobs_api_v1_jobs_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/jobs/{job_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one evaluation job's status, progress and error
         * @description Returns the job if it belongs to the caller's college, else 404.
         *
         *     `job_id` is typed as uuid.UUID so a malformed id is rejected by FastAPI as
         *     a 422 before any SQL runs — a non-uuid string reaching the query would
         *     raise inside Postgres's cast and surface as a 500.
         */
        get: operations["get_job_api_v1_jobs__job_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/evaluate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Queue a booklet for evaluation
         * @description Queues the work this booklet needs and returns 202 with the job id.
         *
         *     Nothing is scored inside this request. Booklet evaluation runs OCR, layout
         *     detection and LLM calls over every region and takes MINUTES (§7D) — past
         *     any sane HTTP timeout, and it would pin a worker thread and a DB
         *     connection for the duration.
         *
         *     TWO SHAPES, and the response says which one happened:
         *
         *       * booklet has no persisted regions -> one `booklet_ingest` job, which
         *         segments the PDF into answer_blocks and then chains into the
         *         `booklet_eval` job whose id appears in its `result`.
         *       * booklet is already ingested      -> one `booklet_eval` job directly,
         *         re-using the regions that exist.
         *
         *     The second case is what makes re-scoring cheap AND safe: ingestion is not
         *     re-run, so a student's answer_blocks are written once and never rewritten
         *     by a re-score (§7C, and core/booklet_pipeline.py on why a second ingestion
         *     would duplicate rather than replace them). This endpoint used to require
         *     that someone had run scripts/ingest_booklet.py by hand first; it no longer
         *     does, and NoRegionsError is now a real failure rather than the normal path.
         */
        post: operations["evaluate_api_v1_evaluate_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/results": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List and filter this college's answer results
         * @description One row per answer — score at a glance, not the full report.
         *
         *     `GET /results/{answer_id}` is the full per-answer report (ledger history,
         *     every review, every region component); this is the review-queue /
         *     dashboard view over many answers at once. Tenant-scoped like every other
         *     answer-schema read — see api/services/evaluation.py::list_results and
         *     core/results.py for the query and why `needs_review` is computed rather
         *     than stored.
         */
        get: operations["list_results_api_v1_results_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/results/{answer_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Full evaluation report for one answer
         * @description The current score, its per-signal breakdown, per-region components,
         *     confidence, the whole append-only ledger history, and every review.
         *
         *     An answer belonging to another college is 404 — identical to a nonexistent
         *     id, so the endpoint cannot be used to discover which answer ids are real
         *     elsewhere on the platform.
         */
        get: operations["get_result_api_v1_results__answer_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/results/{answer_id}/pages/{page_number}/image": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A short-lived URL for one scanned page of this answer
         * @description The scanned page a region was cut from, as a SHORT-LIVED PRESIGNED URL.
         *
         *     This is the left half of the Results screen. `region_bbox` on each entry in
         *     GET /results/{answer_id}'s `regions` is in pixel coordinates of exactly
         *     this image (the DESKEWED page — migration 013), so an overlay drawn from
         *     one lands on the other with no transform.
         *
         *     A URL RATHER THAN THE BYTES. A 200-DPI deskewed page is multi-megabyte;
         *     streaming it would pin an API worker and its database connection for the
         *     whole transfer, per page, per reviewer. The reasoning, and what that choice
         *     costs, is in api/services/evaluation.py::resolve_page_image. The URL
         *     expires in PAGE_IMAGE_URL_TTL_SECONDS (300s) and is generated per request:
         *     nothing writes it back, because the database stores stable "bucket/key"
         *     references and never presigned ones (CLAUDE_CONTEXT.md §10).
         *
         *     THE PAGE IS REACHED THROUGH THE ANSWER — `answers` JOIN `answer_blocks`,
         *     with this college's id in the predicate. A page_image_url is never taken
         *     from a caller. Another college's answer is 404, byte-identical to a
         *     nonexistent answer id, to a page this booklet does not have, and to a page
         *     whose image was never stored: four causes, one response, no oracle.
         */
        get: operations["get_page_image_api_v1_results__answer_id__pages__page_number__image_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/results/{answer_id}/override": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Record a teacher's override of an AI score
         * @description Appends an answer_reviews row and moves the answer to 'sme_reviewed'.
         *
         *     THE REVIEW IS ATTRIBUTED TO THE AUTHENTICATED CALLER — `reviewer_id` comes
         *     from the access token and is not a request field (RE-4). An audit trail
         *     whose subject the caller picks is not an audit trail.
         *
         *     **Writes NOTHING to evaluation_results.** The ledger records what the AI
         *     computed; the model really did output that number, and overwriting it
         *     would erase the evidence needed to tell whether the model is
         *     systematically wrong. The override is a new fact in answer_reviews, and
         *     GET /results/{answer_id} reconciles the two into `final_marks` at read
         *     time. tests/test_api/test_evaluation.py asserts the evaluation_results row
         *     count and every row's contents are byte-identical before and after this
         *     call.
         */
        post: operations["override_result_api_v1_results__answer_id__override_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/questions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List and filter the question bank
         * @description A filtered page of the shared bank, newest first.
         *
         *     `status` is aliased from a parameter named `status_filter` because
         *     `status` is also the FastAPI status module imported in this file; the
         *     alias keeps the wire name right without shadowing it.
         *
         *     limit/offset now come from the shared api/deps/pagination.py dependency,
         *     the same one every list endpoint added in this pass uses — this endpoint
         *     used to declare its own `Query(le=200)`, which REJECTED a limit above
         *     MAX_LIMIT with 422. The shared dependency CLAMPS instead (see its
         *     docstring); this is the one behavioural change from bringing /questions
         *     in line with the rest.
         */
        get: operations["list_questions_api_v1_questions_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/questions/{question_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One question, with its source paragraph and review history
         * @description Everything a reviewer needs before deciding: the question, the content
         *     it was generated from, and every prior review — the same payload
         *     `review_question.py --show` prints.
         */
        get: operations["get_question_api_v1_questions__question_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/questions/generate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Generate draft questions from a paragraph
         * @description Creates `count` questions at status='draft' and returns them.
         *
         *     201, not 202: unlike booklet evaluation this runs inline. Generating a
         *     handful of questions is one LLM call, and the caller needs the ids back to
         *     put them in front of a reviewer — a job id would make the very next step
         *     (review) a polling loop for no benefit.
         *
         *     Every question returned is a DRAFT. None of them is usable in a paper
         *     until it has been through both gates below.
         */
        post: operations["generate_questions_api_v1_questions_generate_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/questions/{question_id}/review": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * QUALITY GATE — confirm or reject a draft question
         * @description draft -> confirmed | rejected. Fires only from 'draft'.
         *
         *     Re-reviewing an already-decided question is a 409, not an overwrite. That
         *     is the CLI's behaviour too, and it is deliberate: changing a decision that
         *     is already recorded in question_reviews is a re-open operation with its
         *     own audit meaning, which nothing in this codebase implements yet. Silently
         *     allowing it here would let a second reviewer erase the first one's
         *     judgement with no trace that it happened.
         *
         *     Writes two rows: question_reviews (who decided what, and why) and
         *     question_status_history (the transition). Both come from
         *     review_question.py; neither is written here.
         */
        post: operations["review_question_api_v1_questions__question_id__review_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/questions/{question_id}/promote": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * PUBLISH GATE — make a confirmed question live
         * @description confirmed -> live. Fires only from 'confirmed'.
         *
         *     THIS IS THE ENDPOINT THAT MUST NOT BE MADE LENIENT. Posting it against a
         *     draft is a 409 and the question stays a draft: the request is refused, not
         *     queued, not upgraded, not partially applied. `promote` is a separate
         *     operational decision from `review` — one says the content is correct, the
         *     other says students may now be asked it — and collapsing them would mean
         *     one person's click puts an unreviewed AI-generated question into an exam.
         *
         *     TAKES NO REQUEST BODY. Its only field was `reviewer_id`, which RE-4
         *     deleted — the promoting reviewer is the authenticated caller.
         *
         *     No question_reviews row is written; promotion is not a content judgement.
         *     The transition IS recorded in question_status_history, so the audit trail
         *     is complete either way.
         */
        post: operations["promote_question_api_v1_questions__question_id__promote_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/papers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List and filter the shared bank of generated papers
         * @description generated_papers is SHARED across colleges, exactly like /questions —
         *     see api/services/papers.py's header. get_tenant_conn authenticates the
         *     caller; it does not scope this query.
         */
        get: operations["list_papers_api_v1_papers_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/papers/generate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Generate an exam paper from a pattern using live questions
         * @description Assigns a live question to every leaf slot the bank can fill.
         *
         *     The paper is attributed to the authenticated caller (`generated_by`), not
         *     to a reviewer named in the body — see PaperGenerateRequest (RE-4).
         *
         *     A successful response can still carry `warnings`: an OPTIONAL slot with no
         *     match leaves a real hole in a valid paper, and the caller is told which
         *     one rather than being handed a quietly shorter paper. A MANDATORY slot
         *     with no match is a 409 — there is no valid paper to return, and the
         *     message names each slot so someone knows which questions must be written,
         *     reviewed and promoted before this pattern can be used.
         */
        post: operations["generate_paper_api_v1_papers_generate_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/exams": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List this college's exams */
        get: operations["list_exams_api_v1_exams_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/students": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List this college's students */
        get: operations["list_students_api_v1_students_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * AssignedSlot
         * @description One LEAF slot: the level at which a question is actually assigned.
         *
         *     `assigned` false with a null question_id is a real, expected state for an
         *     optional section — the slot exists in the pattern and the bank had nothing
         *     that fits. It is reported rather than silently dropped, because a caller
         *     rendering the paper needs to show the gap to whoever must fill it.
         */
        AssignedSlot: {
            /**
             * Slot Id
             * Format: uuid
             */
            slot_id: string;
            /** Slot Label */
            slot_label: string;
            /** Marks */
            marks: number;
            /** Style */
            style: string;
            /** Assigned */
            assigned: boolean;
            /** Question Id */
            question_id?: string | null;
            /** Question Content */
            question_content?: string | null;
            /**
             * Question Marks
             * @description The assigned question's own marks_max. Differs from `marks` when it was matched within marks_tolerance rather than exactly — that difference is the caller's to notice.
             */
            question_marks?: number | null;
        };
        /** Body_upload_booklet_api_v1_upload_post */
        Body_upload_booklet_api_v1_upload_post: {
            /**
             * File
             * @description The scanned answer booklet, as a PDF.
             */
            file: string;
        };
        /** CappedList[LedgerEntry] */
        CappedList_LedgerEntry_: {
            /** Items */
            items: components["schemas"]["LedgerEntry"][];
            /**
             * Total
             * @description The TRUE count before capping, not len(items).
             */
            total: number;
            /**
             * Truncated
             * @description True when `total` exceeds `items` — more rows exist than were returned.
             */
            truncated: boolean;
        };
        /** CappedList[RegionDetail] */
        CappedList_RegionDetail_: {
            /** Items */
            items: components["schemas"]["RegionDetail"][];
            /**
             * Total
             * @description The TRUE count before capping, not len(items).
             */
            total: number;
            /**
             * Truncated
             * @description True when `total` exceeds `items` — more rows exist than were returned.
             */
            truncated: boolean;
        };
        /** CappedList[ReviewEntry] */
        CappedList_ReviewEntry_: {
            /** Items */
            items: components["schemas"]["ReviewEntry"][];
            /**
             * Total
             * @description The TRUE count before capping, not len(items).
             */
            total: number;
            /**
             * Truncated
             * @description True when `total` exceeds `items` — more rows exist than were returned.
             */
            truncated: boolean;
        };
        /** CappedList[dict[str, Any]] */
        CappedList_dict_str__Any__: {
            /** Items */
            items: {
                [key: string]: unknown;
            }[];
            /**
             * Total
             * @description The TRUE count before capping, not len(items).
             */
            total: number;
            /**
             * Truncated
             * @description True when `total` exceeds `items` — more rows exist than were returned.
             */
            truncated: boolean;
        };
        /**
         * ConfidenceBlock
         * @description Two DIFFERENT axes of confidence, kept apart deliberately.
         *
         *     `question`/`booklet` are evaluation confidence — how sure the scorer is of
         *     the mark. Each region's `classification_confidence` is how sure the layout
         *     model was about what KIND of region it is. A region can be confidently a
         *     table and still be read badly (migration 013 spells this out), and the two
         *     failures need different fixes, so they are never merged into one number.
         */
        ConfidenceBlock: {
            /** Question */
            question?: number | null;
            /** Booklet */
            booklet?: number | null;
            /**
             * Region Counts
             * @description {total, evaluated, failed, flagged} for this question. `total - evaluated` is how much of the answer was never scored — the gap §7D keeps visible rather than folding into a zero.
             */
            region_counts?: {
                [key: string]: number;
            };
            /**
             * Regions
             * @description Per-region identity (block_id, question, block_type, page, bbox), flattened out of the components so a reviewer can find each region on its original page.
             */
            regions?: {
                [key: string]: unknown;
            }[];
        };
        /**
         * EvaluateRequest
         * @description POST /api/v1/evaluate.
         *
         *     The first three ids are what identifies a booklet in this schema. There is
         *     no booklets table and no exams.paper_id column (§7C's open schema gap), so
         *     core/booklet_evaluator.load_booklet_tasks addresses a booklet as "the
         *     answers one student wrote for one exam", optionally narrowed to a single
         *     ingestion. upload_id supplies that narrowing by resolving to the stored
         *     scan's blob_url — which is why all three are required rather than
         *     exam+student alone.
         *
         *     paper_id is the fourth, and it is required for the same missing FK: the
         *     question markers this booklet's regions carry ('Q1', 'Q2a') are resolved
         *     against pattern_slots.slot_label through one specific generated paper,
         *     labels repeat across papers, and the exam cannot name its paper. See
         *     api/services/ingestion.py's docstring — this field is what replaces the
         *     CLI's --paper-id, and it is what would become optional if
         *     `exams.paper_id` were ever added (§10, a product decision).
         */
        EvaluateRequest: {
            /**
             * Upload Id
             * Format: uuid
             * @description From POST /api/v1/upload.
             */
            upload_id: string;
            /**
             * Exam Id
             * Format: uuid
             */
            exam_id: string;
            /**
             * Student Id
             * Format: uuid
             */
            student_id: string;
            /**
             * Paper Id
             * Format: uuid
             * @description generated_papers.paper_id whose slot labels the booklet's question markers resolve against. Required because there is no FK from exams to generated_papers (§7C). Still required when the booklet is already ingested — it is validated either way, so a wrong paper is a 404 now rather than a mis-associated re-ingest later.
             */
            paper_id: string;
            /**
             * Stub
             * @description DEVELOPMENT ONLY — score with the plugins' stub paths instead of the real models and Groq. Rejected with 403 unless debug endpoints are enabled, because a stub score is a FABRICATED score and it lands in the append-only ledger like any other.
             * @default false
             */
            stub: boolean;
            /**
             * Stub Llm
             * @description DEVELOPMENT ONLY — skip only the LLM call. Same 403 gate as `stub`.
             * @default false
             */
            stub_llm: boolean;
            /**
             * Method
             * @description Override the text plugin's scoring method (e.g. 'embeddings', 'llm'). Null uses the plugin's blended default.
             */
            method?: string | null;
        };
        /**
         * EvaluateResponse
         * @description 202 Accepted — the work is queued, not done.
         *
         *     202 rather than 201 on purpose: booklet evaluation takes minutes (§7D), so
         *     what was created is a promise to do the work, and the client's next move is
         *     to poll `job_id`. 200 would imply a finished result the response does not
         *     contain.
         */
        EvaluateResponse: {
            /**
             * Job Id
             * Format: uuid
             * @description Poll GET /api/v1/jobs/{job_id}.
             */
            job_id: string;
            /**
             * Job Type
             * @description What `job_id` actually is: 'booklet_ingest' when this booklet had to be segmented first, 'booklet_eval' when it was already ingested and is being re-scored. The client polls the same endpoint either way, but an ingest job's `result` carries the `evaluation_job_id` to poll next.
             */
            job_type: string;
            /**
             * Ingest Job Id
             * @description The ingestion job queued for this booklet, or null when its regions already existed. Equal to job_id when it is not null — named separately so a client can tell 'ingest then evaluate' from 'evaluate' without parsing job_type.
             */
            ingest_job_id?: string | null;
            /**
             * Status
             * @description Always 'queued' at this point.
             * @enum {string}
             */
            status: "queued" | "running" | "succeeded" | "failed";
            /**
             * Upload Id
             * Format: uuid
             */
            upload_id: string;
            /**
             * Exam Id
             * Format: uuid
             */
            exam_id: string;
            /**
             * Student Id
             * Format: uuid
             */
            student_id: string;
            /**
             * Paper Id
             * Format: uuid
             */
            paper_id: string;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
        };
        /** EvaluationBlock */
        EvaluationBlock: {
            /**
             * Evaluation Id
             * Format: uuid
             */
            evaluation_id: string;
            /** Score */
            score: number;
            /** Explanation */
            explanation?: string | null;
            /** Evaluator Type */
            evaluator_type: string;
            /** Evaluator Model */
            evaluator_model?: string | null;
            /**
             * Evaluated At
             * Format: date-time
             */
            evaluated_at: string;
            /** Plugin */
            plugin?: string | null;
            /** Plugin Version */
            plugin_version?: string | null;
            /** Reference Answer Variant Id */
            reference_answer_variant_id?: string | null;
            /**
             * Reference Asset Id
             * @description Exactly one of the two reference ids is set — evaluation_results is polymorphic over them (migration 012).
             */
            reference_asset_id?: string | null;
        };
        /** ExamSummary */
        ExamSummary: {
            /**
             * Exam Id
             * Format: uuid
             */
            exam_id: string;
            /** Name */
            name: string;
            /** Conducted At */
            conducted_at?: string | null;
            /**
             * Status
             * @enum {string}
             */
            status: "draft" | "live" | "closed";
        };
        /**
         * FinalMarks
         * @description The mark that actually stands, and where it came from.
         *
         *     Computed at READ time from the ledger plus the review log, never stored.
         *     Storing it would mean UPDATEing a row whenever a review arrives, and both
         *     source tables are append-only by design.
         */
        FinalMarks: {
            /** Marks */
            marks?: number | null;
            /** Source */
            source?: ("ai" | "sme_override") | null;
            /** Review Id */
            review_id?: string | null;
            /**
             * Ai Score
             * @description The AI's score, kept alongside an override so the disagreement stays visible rather than being overwritten.
             */
            ai_score?: number | null;
        };
        /**
         * GenerateQuestionsRequest
         * @description POST /api/v1/questions/generate.
         *
         *     There is no `status` field and there will not be one. Generation always
         *     produces status='draft' (scripts/generate_questions.py hardcodes it in the
         *     INSERT), because every question-creation path in this codebase feeds the
         *     mandatory review gate. A request field that could name a starting status
         *     would be the skip-review path this API is explicitly not allowed to have.
         */
        GenerateQuestionsRequest: {
            /**
             * Paragraph Id
             * Format: uuid
             * @description The already-uploaded paragraph to generate from. Must be status='active' — generating from superseded content is refused.
             */
            paragraph_id: string;
            /**
             * Count
             * @description How many questions to generate. Capped at 20: each one is an LLM-produced draft a human then has to read.
             * @default 3
             */
            count: number;
            /**
             * Style
             * @description Constrain every generated question to this style. Null lets the model choose per question.
             */
            style?: ("long" | "short" | "one_word" | "mcq") | null;
            /**
             * Intent Hint
             * @description Teacher guidance, e.g. "focus on definitions, easy difficulty" — the intent-hinted generation approach.
             */
            intent_hint?: string | null;
            /**
             * Stub Llm
             * @description Development only: skip the real LLM and emit deterministic placeholder text. Rejected with 403 unless debug endpoints are enabled.
             * @default false
             */
            stub_llm: boolean;
        };
        /** GenerateQuestionsResponse */
        GenerateQuestionsResponse: {
            /**
             * Paragraph Id
             * Format: uuid
             */
            paragraph_id: string;
            /** Count */
            count: number;
            /** Questions */
            questions: components["schemas"]["GeneratedQuestion"][];
        };
        /**
         * GeneratedQuestion
         * @description One freshly created draft.
         */
        GeneratedQuestion: {
            /**
             * Question Id
             * Format: uuid
             */
            question_id: string;
            /**
             * Question Group Id
             * Format: uuid
             */
            question_group_id: string;
            /**
             * Status
             * @description Always 'draft'. Typed as a single-value Literal so that if generation ever started returning anything else, this response would fail validation loudly instead of quietly reporting that review had been skipped.
             * @constant
             */
            status: "draft";
            /**
             * Style
             * @enum {string}
             */
            style: "long" | "short" | "one_word" | "mcq";
            /** Marks Max */
            marks_max: number;
            /** Content */
            content: string;
        };
        /** HTTPValidationError */
        HTTPValidationError: {
            /** Detail */
            detail?: components["schemas"]["ValidationError"][];
        };
        /** HealthResponse */
        HealthResponse: {
            /** Status */
            status: string;
        };
        /**
         * JobProgress
         * @description Where a job has got to.
         *
         *     `stage` and `percent` come from evaluation_jobs.progress, which the worker
         *     writes between phases (migration 015). They are STAGE MARKERS, not measured
         *     fractions, and the distinction is deliberate: core/booklet_evaluator runs
         *     extraction and evaluation as bounded thread pools with no progress
         *     callback, so completion *within* a phase is genuinely unknown. Reporting
         *     which phase a job is in is honest. Interpolating a percentage inside one
         *     would be a fabricated number that a client would render as a smoothly
         *     moving bar — and it would hide, from us, that real progress reporting does
         *     not exist. Adding a callback to core/ is what would make these real.
         *
         *     `counts` carries whatever the stage actually knows (regions, questions), so
         *     a client has something true to display alongside the coarse marker.
         */
        JobProgress: {
            /**
             * Stage
             * @description Machine-readable phase. Which phases are possible depends on the job's type, and the two are deliberately distinguishable: an INGEST job (booklet_ingest) reports checking, fetching, rasterizing, segmenting, persisting; an EVALUATION job (booklet_eval) reports loading, evaluating, persisting. Plus queued, done and failed for both. A client showing 'persisting' should read job_type to know whether that is answer_blocks or ledger rows.
             */
            stage: string;
            /**
             * Percent
             * @description Stage marker in [0, 1] — NOT a measured fraction of work done. Null when the stage is unknown.
             */
            percent?: number | null;
            /**
             * Message
             * @description Human-readable one-liner about the current state.
             */
            message: string;
            /**
             * Attempts
             * @description How many times this job has been CLAIMED by a worker. >1 means it was retried, which usually means it died mid-run.
             */
            attempts: number;
            /**
             * Counts
             * @description What the current stage knows, e.g. {'regions': 10, 'questions': 5}. Empty until a stage reports something.
             */
            counts?: {
                [key: string]: number;
            };
        };
        /**
         * JobResponse
         * @description One job as reported by GET /api/v1/jobs/{job_id}.
         *
         *     college_id is deliberately NOT exposed. The caller already knows their own
         *     tenant (they authenticated as it), a job from any other tenant returns 404
         *     rather than a body, so the field could only ever echo the request — and
         *     every field an API returns is a field a future endpoint might be tempted
         *     to accept.
         */
        JobResponse: {
            /**
             * Job Id
             * Format: uuid
             */
            job_id: string;
            /** Job Type */
            job_type: string;
            /**
             * Status
             * @enum {string}
             */
            status: "queued" | "running" | "succeeded" | "failed";
            progress: components["schemas"]["JobProgress"];
            /**
             * Error
             * @description Why the job failed. Null unless status is 'failed'.
             */
            error?: string | null;
            /**
             * Result
             * @description The worker's run report, once terminal. NOT the scores — those are appended to the evaluation_results ledger.
             */
            result?: {
                [key: string]: unknown;
            } | null;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /** Started At */
            started_at?: string | null;
            /** Finished At */
            finished_at?: string | null;
        };
        /**
         * LedgerEntry
         * @description One row of the append-only score history.
         *
         *     The whole history is returned, not just the current row, because
         *     evaluation_results is append-only (rule 2) — a re-scored answer keeps
         *     every previous score, and `is_current` moves rather than rows being
         *     replaced. That audit trail is the point of the rule, so the API surfaces
         *     it instead of hiding everything but the latest.
         */
        LedgerEntry: {
            /**
             * Evaluation Id
             * Format: uuid
             */
            evaluation_id: string;
            /** Score */
            score: number;
            /** Evaluator Type */
            evaluator_type: string;
            /** Evaluator Model */
            evaluator_model?: string | null;
            /** Is Current */
            is_current: boolean;
            /**
             * Evaluated At
             * Format: date-time
             */
            evaluated_at: string;
        };
        /**
         * LoginRequest
         * @description POST /api/v1/auth/login.
         *
         *     `email` is a plain `str`, NOT pydantic's `EmailStr`, and that is a
         *     decision rather than an omission. `EmailStr` needs the `email-validator`
         *     package, which this repo would be adding for one field; and it would make
         *     a syntactically invalid address answer 422 while a valid-but-unknown one
         *     answers 401. Two shapes of "no" on the login endpoint is precisely what
         *     the rest of this router works to avoid, so every address that is not blank
         *     takes the same path and gets the same answer at the same speed. The
         *     database is the authority on the identifier anyway (citext UNIQUE, plus
         *     users_email_not_blank).
         */
        LoginRequest: {
            /** Email */
            email: string;
            /**
             * Password
             * @description The account password. Never logged, never echoed.
             */
            password: string;
        };
        /**
         * LogoutRequest
         * @description POST /api/v1/auth/logout.
         *
         *     Takes the refresh token in the body rather than reading the access token,
         *     because the access token is not what logout acts on: revoking the refresh
         *     token is the only part of a session the server can actually end.
         */
        LogoutRequest: {
            /** Refresh Token */
            refresh_token: string;
            /**
             * All Sessions
             * @description Revoke every live session for this account, not just this one — the 'sign out everywhere' button. Requires the presented refresh token to be live, so it cannot be used to log someone else out.
             * @default false
             */
            all_sessions: boolean;
        };
        /**
         * MergedText
         * @description The text that was actually scored, and which region each part came from.
         *
         *     §7D decision 3: a question's text regions are ONE answer, concatenated in
         *     reading order and evaluated once — scoring each half of a page-spanning
         *     answer against the whole reference would mark a complete answer twice as
         *     incomplete. So the merged string is what the score is about, and the
         *     per-part offsets are what let a teacher see the seam.
         *
         *     NEVER PARTIAL. `available` is false with a `reason` when any part's text is
         *     missing, rather than returning a merge with a hole in it — an answer
         *     missing its second page would otherwise read as a complete answer that
         *     simply says less, which is the exact failure the merge exists to prevent.
         */
        MergedText: {
            /** Text */
            text?: string | null;
            /** Available */
            available: boolean;
            /**
             * Reason
             * @description Why `text` is null. Null when available.
             */
            reason?: string | null;
            /**
             * Separator
             * @description What the parts were joined with — the evaluator's own TEXT_MERGE_SEPARATOR, not a value this layer chose.
             */
            separator: string;
            /** Block Ids */
            block_ids?: string[];
            /** Parts */
            parts?: components["schemas"]["MergedTextPart"][];
        };
        /**
         * MergedTextPart
         * @description Where one region's text sits inside the merged answer — THE SEAM.
         */
        MergedTextPart: {
            /**
             * Block Id
             * Format: uuid
             */
            block_id: string;
            /** Page Number */
            page_number?: number | null;
            /** Reading Order */
            reading_order: number;
            /**
             * Offset
             * @description Start index of this region's text in `merged_text.text`.
             */
            offset: number;
            /** Length */
            length: number;
        };
        /**
         * OverrideRequest
         * @description POST /api/v1/results/{answer_id}/override.
         *
         *     THERE IS NO `reviewer_id` FIELD, and there must never be one again. It was
         *     here while identity was a stub that knew a college but not a person, and
         *     it meant a client could attribute a review to any reviewer in its own
         *     college — migration 003's trg_answer_reviews_derive_and_check_college
         *     refuses only reviewers from OTHER colleges, so the whole of one college's
         *     staff was impersonable by any of them. The reviewer is now
         *     `CurrentUser.reviewer_id`, out of the signed token
         *     (api/deps/identity.py, RE-4).
         *
         *     `extra="forbid"` is what makes that stick: a client still sending the old
         *     field gets a 422 naming it, rather than having it silently ignored while
         *     the review is attributed to someone else than they asked for.
         */
        OverrideRequest: {
            /**
             * Action
             * @description 'overridden' changes the mark, 'confirmed' agrees with the AI, 'flagged' escalates without setting one.
             * @default overridden
             * @enum {string}
             */
            action: "confirmed" | "overridden" | "flagged";
            /**
             * Final Marks
             * @description The mark that should stand. Required for 'overridden'; must be omitted for 'flagged', which by definition has not decided on a mark.
             */
            final_marks?: number | null;
            /** Comment */
            comment?: string | null;
        };
        /** OverrideResponse */
        OverrideResponse: {
            /**
             * Review Id
             * Format: uuid
             */
            review_id: string;
            /**
             * Answer Id
             * Format: uuid
             */
            answer_id: string;
            /**
             * Reviewer Id
             * Format: uuid
             */
            reviewer_id: string;
            /**
             * Action
             * @enum {string}
             */
            action: "confirmed" | "overridden" | "flagged";
            /** Final Marks */
            final_marks?: number | null;
            /** Comment */
            comment?: string | null;
            /**
             * Reviewed At
             * Format: date-time
             */
            reviewed_at: string;
            /** Previous Status */
            previous_status: string;
            /** Status */
            status: string;
            /**
             * Status Changed
             * @description False when the answer was already 'sme_reviewed' (a second override) or is 'finalized' (no path back out — an open product decision, not settled here). The review row is written either way.
             */
            status_changed: boolean;
            /**
             * Evaluation Results Untouched
             * @description Always true, and stated in the response on purpose: an override NEVER writes to evaluation_results. That ledger records what the AI computed and is append-only (rule 2); a teacher's disagreement is a new fact in answer_reviews, not a correction to the model's output. GET /results/{answer_id} reconciles the two at read time.
             * @default true
             * @constant
             */
            evaluation_results_untouched: true;
        };
        /**
         * PageImageResponse
         * @description GET /api/v1/results/{answer_id}/pages/{page_number}/image.
         *
         *     A SHORT-LIVED PRESIGNED URL, not the bytes — see
         *     api/services/evaluation.py::resolve_page_image for the argument. The URL is
         *     generated per request and is stored NOWHERE: the database keeps the stable
         *     "bucket/key" reference and never a presigned one (CLAUDE_CONTEXT.md §10).
         */
        PageImageResponse: {
            /**
             * Answer Id
             * Format: uuid
             */
            answer_id: string;
            /** Page Number */
            page_number: number;
            /**
             * Url
             * @description Fetch it directly from the browser. Treat it as a credential: it grants read access to this one object until it expires, so do not log it or put it in a shareable link.
             */
            url: string;
            /**
             * Expires In
             * @description Seconds this URL stays valid — short by design, and shorter than the access token's own lifetime.
             */
            expires_in: number;
            /**
             * Expires At
             * Format: date-time
             */
            expires_at: string;
        };
        /**
         * PageSummary
         * @description One page of this answer's booklet, for the page strip.
         */
        PageSummary: {
            /** Page Number */
            page_number: number;
            /** Regions */
            regions: number;
            /**
             * Has Image
             * @description Whether GET /results/{answer_id}/pages/{page_number}/image has anything to serve — so the client does not have to probe it per page to find out.
             */
            has_image: boolean;
            /**
             * Needs Review
             * @description True when at least one region on this page is flagged.
             */
            needs_review: boolean;
        };
        /** Page[ExamSummary] */
        Page_ExamSummary_: {
            /** Items */
            items: components["schemas"]["ExamSummary"][];
            /**
             * Total
             * @description Rows matching the filters, ignoring limit/offset — lets a client page without guessing when it has reached the end.
             */
            total: number;
            /** Limit */
            limit: number;
            /** Offset */
            offset: number;
        };
        /** Page[JobResponse] */
        Page_JobResponse_: {
            /** Items */
            items: components["schemas"]["JobResponse"][];
            /**
             * Total
             * @description Rows matching the filters, ignoring limit/offset — lets a client page without guessing when it has reached the end.
             */
            total: number;
            /** Limit */
            limit: number;
            /** Offset */
            offset: number;
        };
        /** Page[PaperSummary] */
        Page_PaperSummary_: {
            /** Items */
            items: components["schemas"]["PaperSummary"][];
            /**
             * Total
             * @description Rows matching the filters, ignoring limit/offset — lets a client page without guessing when it has reached the end.
             */
            total: number;
            /** Limit */
            limit: number;
            /** Offset */
            offset: number;
        };
        /** Page[ResultSummary] */
        Page_ResultSummary_: {
            /** Items */
            items: components["schemas"]["ResultSummary"][];
            /**
             * Total
             * @description Rows matching the filters, ignoring limit/offset — lets a client page without guessing when it has reached the end.
             */
            total: number;
            /** Limit */
            limit: number;
            /** Offset */
            offset: number;
        };
        /** Page[StudentSummary] */
        Page_StudentSummary_: {
            /** Items */
            items: components["schemas"]["StudentSummary"][];
            /**
             * Total
             * @description Rows matching the filters, ignoring limit/offset — lets a client page without guessing when it has reached the end.
             */
            total: number;
            /** Limit */
            limit: number;
            /** Offset */
            offset: number;
        };
        /** Page[UploadSummary] */
        Page_UploadSummary_: {
            /** Items */
            items: components["schemas"]["UploadSummary"][];
            /**
             * Total
             * @description Rows matching the filters, ignoring limit/offset — lets a client page without guessing when it has reached the end.
             */
            total: number;
            /** Limit */
            limit: number;
            /** Offset */
            offset: number;
        };
        /**
         * PaperGenerateRequest
         * @description POST /api/v1/papers/generate.
         */
        PaperGenerateRequest: {
            /**
             * Pattern Id
             * Format: uuid
             * @description The paper_pattern to fill. Must be is_active — generating from a retired pattern is refused.
             */
            pattern_id: string;
            /**
             * Name
             * @description Human-readable paper name, e.g. "AIML CIA-2 Aug 2026".
             */
            name: string;
            /**
             * Choose Count
             * @description Default choose_count for OPTIONAL sections — how many of the section's questions a student must answer. Mandatory sections ignore it and always use their full slot count.
             * @default 1
             */
            choose_count: number;
            /**
             * Marks Tolerance
             * @description How far a question's marks_max may sit from the slot's marks when no exact match exists. 0 disables fuzzy matching, making generation exact-or-fail.
             * @default 1
             */
            marks_tolerance: number;
        };
        /**
         * PaperGenerateResponse
         * @description The generated paper, as persisted.
         */
        PaperGenerateResponse: {
            /**
             * Paper Id
             * Format: uuid
             */
            paper_id: string;
            /**
             * Pattern Id
             * Format: uuid
             */
            pattern_id: string;
            /** Pattern Name */
            pattern_name: string;
            /** Name */
            name: string;
            /**
             * Status
             * @enum {string}
             */
            status: "draft" | "finalized";
            /** Total Filled */
            total_filled: number;
            /** Total Slots */
            total_slots: number;
            /**
             * Is Complete
             * @description False iff `warnings` is non-empty — every LEAF slot in the pattern got a question. Equivalent to `not warnings`, surfaced as its own field so a client can check completeness without inspecting the warnings list.
             */
            is_complete: boolean;
            /**
             * Filled Marks
             * @description Sum of the MARKS of every filled slot (the slot's own marks value, not the assigned question's marks_max, which can differ slightly under marks_tolerance fuzzy matching). Compare against `pattern_total_marks` to see how much of the paper's intended marks are covered.
             */
            filled_marks: number;
            /**
             * Pattern Total Marks
             * @description paper_patterns.total_marks for the pattern this paper was generated from — the full marks the pattern is worth when every slot is filled.
             */
            pattern_total_marks: number;
            /** Sections */
            sections: components["schemas"]["PaperSection"][];
            /**
             * Warnings
             * @description One entry per unfilled OPTIONAL slot. A paper with warnings is valid but incomplete (is_complete=false); unfilled MANDATORY slots are a 409, not a warning — see this module's header.
             */
            warnings?: components["schemas"]["SlotWarning"][];
        };
        /** PaperSection */
        PaperSection: {
            /** Section Label */
            section_label: string;
            /** Section Order */
            section_order: number;
            /** Is Mandatory */
            is_mandatory: boolean;
            /** Choose Count */
            choose_count: number;
            /** Slots */
            slots: components["schemas"]["PaperSlot"][];
        };
        /**
         * PaperSlot
         * @description A top-level slot. Parent slots (Q2 = Q2a + Q2b) are STRUCTURAL and
         *     carry no question of their own — only their leaves do, which is why
         *     `leaves` exists even for a standalone slot (it holds exactly itself).
         */
        PaperSlot: {
            /** Slot Label */
            slot_label: string;
            /** Marks */
            marks: number;
            /** Style */
            style: string;
            /** Is Parent */
            is_parent: boolean;
            /** Leaves */
            leaves: components["schemas"]["AssignedSlot"][];
        };
        /**
         * PaperSummary
         * @description One row of GET /api/v1/papers.
         *
         *     No college_id here either, for the identical reason
         *     api/schemas/questions.py's header gives: generated_papers has no such
         *     column (migration 008 line 15 — shared, no RLS), so a college_id field
         *     would be a fabrication a future endpoint could be tempted to filter on.
         */
        PaperSummary: {
            /**
             * Paper Id
             * Format: uuid
             */
            paper_id: string;
            /**
             * Pattern Id
             * Format: uuid
             */
            pattern_id: string;
            /** Name */
            name: string;
            /**
             * Status
             * @enum {string}
             */
            status: "draft" | "finalized";
            /**
             * Generated At
             * Format: date-time
             */
            generated_at: string;
            /**
             * Generated By
             * @description NULL = system-generated.
             */
            generated_by?: string | null;
        };
        /**
         * PriorReview
         * @description One row of a question's question_reviews history.
         */
        PriorReview: {
            /**
             * Reviewer Id
             * Format: uuid
             */
            reviewer_id: string;
            /**
             * Action
             * @description question_review_action enum — the DECISION recorded, which is the past-tense form of the request's `action`.
             * @enum {string}
             */
            action: "confirmed" | "rejected";
            /** Comment */
            comment?: string | null;
            /**
             * Reviewed At
             * Format: date-time
             */
            reviewed_at: string;
        };
        /**
         * QuestionDetail
         * @description One question in full: GET /api/v1/questions/{id}.
         *
         *     Adds the source paragraph's text and the review history, which is exactly
         *     what `scripts/review_question.py --show` puts in front of a human before
         *     they decide. The API showing less than the CLI would push reviewers back
         *     to the terminal to make an informed call.
         */
        QuestionDetail: {
            /**
             * Question Id
             * Format: uuid
             */
            question_id: string;
            /** Content */
            content: string;
            /**
             * Status
             * @enum {string}
             */
            status: "draft" | "confirmed" | "rejected" | "live" | "superseded";
            /** Style */
            style?: ("long" | "short" | "one_word" | "mcq") | null;
            /** Marks Max */
            marks_max?: number | null;
            /** Source Type */
            source_type?: ("sentence" | "paragraph" | "diagram" | "table" | "formula" | "manual") | null;
            /**
             * Source Id
             * @description What this question was generated from — a paragraph_id when source_type is 'paragraph'. Null for manual entries.
             */
            source_id?: string | null;
            /**
             * Is Ai Generated
             * @description Null for rows written before migration 005 added the column; the tri-state is real and is not flattened to false, which would assert human authorship this repo cannot actually vouch for.
             */
            is_ai_generated?: boolean | null;
            /** Question Group Id */
            question_group_id?: string | null;
            /** Parent Question Id */
            parent_question_id?: string | null;
            /** Created At */
            created_at?: string | null;
            /** Source Paragraph Content */
            source_paragraph_content?: string | null;
            /** Prior Reviews */
            prior_reviews?: components["schemas"]["PriorReview"][];
        };
        /**
         * QuestionListResponse
         * @description A page of the bank, plus the total that matched the same filters.
         */
        QuestionListResponse: {
            /** Questions */
            questions: components["schemas"]["QuestionSummary"][];
            /**
             * Total
             * @description Rows matching the filters, ignoring limit/offset — so a client can page without guessing when it has reached the end.
             */
            total: number;
            /** Limit */
            limit: number;
            /** Offset */
            offset: number;
        };
        /**
         * QuestionSummary
         * @description One row of GET /api/v1/questions.
         */
        QuestionSummary: {
            /**
             * Question Id
             * Format: uuid
             */
            question_id: string;
            /** Content */
            content: string;
            /**
             * Status
             * @enum {string}
             */
            status: "draft" | "confirmed" | "rejected" | "live" | "superseded";
            /** Style */
            style?: ("long" | "short" | "one_word" | "mcq") | null;
            /** Marks Max */
            marks_max?: number | null;
            /** Source Type */
            source_type?: ("sentence" | "paragraph" | "diagram" | "table" | "formula" | "manual") | null;
            /**
             * Source Id
             * @description What this question was generated from — a paragraph_id when source_type is 'paragraph'. Null for manual entries.
             */
            source_id?: string | null;
            /**
             * Is Ai Generated
             * @description Null for rows written before migration 005 added the column; the tri-state is real and is not flattened to false, which would assert human authorship this repo cannot actually vouch for.
             */
            is_ai_generated?: boolean | null;
            /** Question Group Id */
            question_group_id?: string | null;
            /** Parent Question Id */
            parent_question_id?: string | null;
            /** Created At */
            created_at?: string | null;
        };
        /**
         * RefreshRequest
         * @description POST /api/v1/auth/refresh — exchange a refresh token for a new pair.
         */
        RefreshRequest: {
            /** Refresh Token */
            refresh_token: string;
        };
        /**
         * RegionDetail
         * @description One persisted region (`answer_blocks` row) of this answer, in reading
         *     order, with what the evaluation did with it.
         *
         *     THIS IS THE SHAPE THE RESULTS SCREEN OVERLAYS ON A PAGE IMAGE: `bbox` is
         *     in pixel coordinates of the DESKEWED page the image endpoint serves (not
         *     of the original PDF page — migration 013's COMMENT on region_bbox says
         *     why the deskewed page is the one that is stored), so a rectangle drawn
         *     from it lands where the region actually is.
         *
         *     `extra="allow"` unlike most models here: the region row gains fields as
         *     the pipeline records more per region (019's extraction provenance is next),
         *     and a client should not 500 on a field the server added.
         */
        RegionDetail: {
            /**
             * Block Id
             * Format: uuid
             */
            block_id: string;
            /**
             * Block Type
             * @description answer_block_type — what a plugin dispatches on. Lossy by design: 20 layout labels collapse into 4 types, which is why classification_label is kept beside it.
             */
            block_type: string;
            /**
             * Page Number
             * @description 1-based page of the source booklet. Null for a block attached from a single image rather than segmented out of a booklet — every row predating booklet ingestion.
             */
            page_number?: number | null;
            /**
             * Region Bbox
             * @description [x, y, w, h] in pixels of the deskewed page image. Feed GET /results/{answer_id}/pages/{page_number}/image to draw it, and use it as the zoom-to-region target.
             */
            region_bbox?: number[] | null;
            /** Sequence Order */
            sequence_order: number;
            /**
             * Reading Order
             * @description 0-based position among this answer's regions, page then sequence. THE ORDER §7D MERGED THE TEXT IN — the same sort key core/booklet_evaluator.build_components uses, so `merged_text.parts` and this agree by construction.
             */
            reading_order: number;
            /**
             * Classification Label
             * @description Raw layout-model label before it was mapped onto block_type ('paragraph_title', 'chart'). The label to debug a mis-route with.
             */
            classification_label?: string | null;
            /**
             * Classification Confidence
             * @description How sure the layout model was of classification_label, in [0,1]. A DIFFERENT axis from ocr_confidence: a region can be confidently a table and still be read badly.
             */
            classification_confidence?: number | null;
            /**
             * Needs Review
             * @description Set at INGESTION (migration 013) — low classification confidence, a block_type no plugin supports, or the heuristic fallback path.
             */
            needs_review: boolean;
            /**
             * Ingestion Flags
             * @description Why ingestion flagged this region, in the same vocabulary core/booklet_evaluator.py uses in its own report.
             */
            ingestion_flags?: string[];
            /**
             * Text
             * @description What this region says — READ `text_source` BEFORE TRUSTING A NULL. Null does NOT mean the region is empty: the text a plugin OCRs out of a scanned region is not persisted anywhere today (ingestion writes answer_blocks.content as NULL and the extraction is dropped after scoring), so a scanned region has no text to return. Only a block whose text was already digital carries one. See core/answer_regions.py's docstring and migrations/019_answer_block_extractions.sql.proposed.
             */
            text?: string | null;
            /**
             * Text Source
             * @description Where `text` came from — 'answer_blocks.content' today. Null means no text is persisted for this region, NOT that the region is blank.
             */
            text_source?: string | null;
            /**
             * Ocr Confidence
             * @description Extraction confidence for THIS region, when one is attributable. Null for a region merged with others: the merged component's confidence is the MINIMUM across its parts (§7D decision 4), and attributing that minimum to each part would misreport every part but the worst.
             */
            ocr_confidence?: number | null;
            /** Ocr Confidence Source */
            ocr_confidence_source?: string | null;
            /**
             * Question
             * @description The question label ingestion assigned this region ('Q2a'), from the paper's slot labels. Not derivable from the answer row alone — there is no FK from exams to generated_papers (§7C).
             */
            question?: string | null;
            /**
             * Scored
             * @description Whether this region ended up inside a component that was actually scored. False with `failure` set is a region that failed; false with no failure is one nothing routed.
             */
            scored: boolean;
            /**
             * Component Index
             * @description Index into `components` of the component this region belongs to.
             */
            component_index?: number | null;
            /** Order In Component */
            order_in_component?: number | null;
            /**
             * Merged With
             * @description How many regions were merged into this region's component, including itself. >1 is the page-break case §7D decision 3 exists for: several regions, ONE scored answer.
             * @default 0
             */
            merged_with: number;
            /** Component Score */
            component_score?: number | null;
            /** Component Confidence */
            component_confidence?: number | null;
            /**
             * Component Is Primary
             * @description True for the component whose score became the question's score (core/booklet_evaluator._pick_primary).
             * @default false
             */
            component_is_primary: boolean;
            /**
             * Failure
             * @description This region's own failure record (stage, error_type, message) when a stage failed on it. Partial failure is the normal case (§7D), so a region that was not scored says why rather than silently missing from the components.
             */
            failure?: {
                [key: string]: unknown;
            } | null;
            /**
             * Has Page Image
             * @description Whether a full-page image is stored for this region's page — i.e. whether the image endpoint can serve it.
             */
            has_page_image: boolean;
            /**
             * Has Region Image
             * @description Whether the CROPPED region itself is stored (blob_url). Separate from has_page_image: the page is what a reviewer sees the region in context on.
             */
            has_region_image: boolean;
        } & {
            [key: string]: unknown;
        };
        /**
         * ResultResponse
         * @description GET /api/v1/results/{answer_id} — the full report for one answer.
         */
        ResultResponse: {
            /**
             * Answer Id
             * Format: uuid
             */
            answer_id: string;
            /**
             * Question Id
             * Format: uuid
             */
            question_id: string;
            /** Question Text */
            question_text?: string | null;
            /**
             * Student Id
             * Format: uuid
             */
            student_id: string;
            /**
             * Exam Id
             * Format: uuid
             */
            exam_id: string;
            /** Status */
            status: string;
            /** Source Scan Url */
            source_scan_url?: string | null;
            /**
             * Submitted At
             * Format: date-time
             */
            submitted_at: string;
            /** Marks Max */
            marks_max?: number | null;
            /** @description The current ledger row. Null when the answer has not been scored yet — which is NOT the same as scoring zero. */
            evaluation?: components["schemas"]["EvaluationBlock"] | null;
            /** @description Text plugin per-signal breakdown. Null for table/diagram answers, which have no signals — not an all-zero object. */
            signals?: components["schemas"]["SignalBreakdown"] | null;
            /** @description Per-region scores that were aggregated into the single question result — one ledger row per QUESTION is decision 1 in core/booklet_evaluator.py, and this is what it preserved. Capped at core/pagination.py::NESTED_MAX — see that module's docstring on why an unbounded nested list still needs a cap even though it isn't a list endpoint. */
            components: components["schemas"]["CappedList_dict_str__Any__"];
            /** @description Every persisted region backing this answer, in reading order, with page/bbox for the overlay, classification, flags, and which component scored it. Capped at NESTED_MAX like the other nested lists — a re-ingest DUPLICATES a booklet's regions rather than replacing them (§7C), so this list has no inherent bound either. */
            regions: components["schemas"]["CappedList_RegionDetail_"];
            /**
             * Pages
             * @description The pages this answer has regions on, and whether each has a stored image to fetch.
             */
            pages?: components["schemas"]["PageSummary"][];
            /** @description The text as it was actually scored PLUS the seam between the regions it came from — both, never only the merged string (§7D decision 3). */
            merged_text: components["schemas"]["MergedText"];
            /** Flags */
            flags?: string[];
            /**
             * Failures
             * @description Per-stage failures recorded during evaluation. Partial failure is the normal case (§7D) — a booklet is not abandoned because one region's OCR failed.
             */
            failures?: {
                [key: string]: unknown;
            }[];
            confidence: components["schemas"]["ConfidenceBlock"];
            /** @description The append-only ledger, newest first, capped at NESTED_MAX rows with the true total alongside — a booklet re-scored often enough must not make this response grow without bound. */
            history: components["schemas"]["CappedList_LedgerEntry_"];
            /** @description Every answer_reviews row, newest first, same cap-and-flag treatment as `history`. */
            reviews: components["schemas"]["CappedList_ReviewEntry_"];
            final_marks: components["schemas"]["FinalMarks"];
        };
        /**
         * ResultSummary
         * @description One row of GET /api/v1/results — NOT the full report.
         *
         *     GET /results/{answer_id} (ResultResponse above) carries the whole ledger
         *     history, every review and every region component; a list of dozens of
         *     answers must not repeat that per row. This is the "which answers match
         *     these filters, and what is their state right now" view — a client that
         *     needs one row's full report still calls GET /results/{answer_id}.
         */
        ResultSummary: {
            /**
             * Answer Id
             * Format: uuid
             */
            answer_id: string;
            /**
             * Question Id
             * Format: uuid
             */
            question_id: string;
            /**
             * Student Id
             * Format: uuid
             */
            student_id: string;
            /**
             * Exam Id
             * Format: uuid
             */
            exam_id: string;
            /**
             * Status
             * @enum {string}
             */
            status: "pending_evaluation" | "ai_scored" | "sme_reviewed" | "finalized" | "flagged";
            /**
             * Submitted At
             * Format: date-time
             */
            submitted_at: string;
            /**
             * Score
             * @description The CURRENT ledger score, or null if this answer has not been evaluated yet — not the same as scoring zero.
             */
            score?: number | null;
            /** Evaluated At */
            evaluated_at?: string | null;
            /**
             * Needs Review
             * @description True when at least one of this answer's answer_blocks carries needs_review — a region-level flag (migration 013), rolled up to the answer for this filter/summary. See core/results.py's docstring.
             */
            needs_review: boolean;
        };
        /** ReviewEntry */
        ReviewEntry: {
            /**
             * Review Id
             * Format: uuid
             */
            review_id: string;
            /**
             * Reviewer Id
             * Format: uuid
             */
            reviewer_id: string;
            /**
             * Action
             * @enum {string}
             */
            action: "confirmed" | "overridden" | "flagged";
            /** Final Marks */
            final_marks?: number | null;
            /** Comment */
            comment?: string | null;
            /**
             * Reviewed At
             * Format: date-time
             */
            reviewed_at: string;
        };
        /**
         * ReviewRequest
         * @description POST /api/v1/questions/{id}/review — the QUALITY gate.
         *
         *     NO `reviewer_id` FIELD (RE-4). It was required here, exactly as
         *     `--reviewer-id` is required on the CLI, because identity was a header
         *     carrying a college rather than a person. It now comes from the access
         *     token: `CurrentUser.reviewer_id`, which is `users.reviewer_id` for the
         *     account that authenticated, so the reviewer recorded in question_reviews
         *     and question_status_history is the human who actually clicked.
         *
         *     The CLI keeps its flag, and that is not an inconsistency: `--reviewer-id`
         *     is how an operator at a terminal, holding database credentials, says who
         *     they are. A CLIENT saying who it is over HTTP is the thing that was wrong.
         */
        ReviewRequest: {
            /**
             * Action
             * @enum {string}
             */
            action: "confirm" | "reject";
            /**
             * Comment
             * @description Optional reviewer note, stored on the question_reviews row.
             */
            comment?: string | null;
        };
        /**
         * SignalBreakdown
         * @description The text plugin's per-signal breakdown: semantic / keyword / llm /
         *     rubric, each a SignalDetail.
         *
         *     Modelled as an open mapping rather than four fixed fields — the plugin owns
         *     which signals exist, and pinning them here would mean this file had to
         *     change every time core/plugins/text_extraction.py grew one. Only the
         *     signals that the plugin reported are present.
         */
        SignalBreakdown: {
            semantic?: components["schemas"]["SignalDetail"] | null;
            keyword?: components["schemas"]["SignalDetail"] | null;
            llm?: components["schemas"]["SignalDetail"] | null;
            rubric?: components["schemas"]["SignalDetail"] | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * SignalDetail
         * @description One scoring signal, in the shape core/booklet_evaluator._signal_summary
         *     actually emits — a score PLUS what produced it and what it counted for.
         *
         *     `score` and `available` are separate fields rather than one nullable
         *     number because a signal that did not run and a signal that ran and scored
         *     zero are different facts about an answer. Collapsing them would read as
         *     "scored, and scored zero" and would quietly drag any average a client
         *     computed over the signals.
         */
        SignalDetail: {
            /** Score */
            score?: number | null;
            /**
             * Model
             * @description Which model or rule set produced this signal.
             */
            model?: string | null;
            /**
             * Weight
             * @description Effective weight after renormalizing over the signals that actually ran — not the configured weight.
             */
            weight?: number | null;
            /**
             * Available
             * @description False when this signal did not run at all (no keywords on the question, an embeddings-only method, a failed LLM call).
             * @default false
             */
            available: boolean;
            /** Explanation */
            explanation?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * SlotWarning
         * @description One unfilled OPTIONAL slot, structured rather than a formatted
         *     sentence — a client renders "Q3 (Part B, 5M): <reason>" itself, or filters
         *     by section, or counts gaps per section, none of which a prose string
         *     supports without parsing it back apart.
         */
        SlotWarning: {
            /** Slot Label */
            slot_label: string;
            /**
             * Section
             * @description The section's label, e.g. 'Part B'.
             */
            section: string;
            /** Marks */
            marks: number;
            /**
             * Reason
             * @description Why no live question was assigned — today, always 'no matching live question' with the style/marks it looked for; kept as text rather than an enum because core/paper_generator.py's matching strategy (exact, then ±marks_tolerance) may grow more failure shapes.
             */
            reason: string;
        };
        /** StudentSummary */
        StudentSummary: {
            /**
             * Student Id
             * Format: uuid
             */
            student_id: string;
            /** Name */
            name: string;
            /** Roll Number */
            roll_number: string;
            /** Email */
            email: string;
            /**
             * Enrolled At
             * Format: date-time
             */
            enrolled_at: string;
        };
        /**
         * TokenResponse
         * @description What login and refresh both return.
         */
        TokenResponse: {
            /** Access Token */
            access_token: string;
            /**
             * Token Type
             * @default bearer
             * @constant
             */
            token_type: "bearer";
            /**
             * Expires In
             * @description Seconds until `access_token` expires. Present so a client never has to decode the JWT to schedule its own refresh.
             */
            expires_in: number;
            /**
             * Expires At
             * Format: date-time
             */
            expires_at: string;
            /**
             * Refresh Token
             * @description Opaque, long-lived, and ROTATED on every refresh: the token used to obtain this response is revoked as part of issuing it, so a stolen refresh token stops working as soon as the legitimate client refreshes with it.
             */
            refresh_token: string;
            /**
             * Refresh Expires At
             * Format: date-time
             */
            refresh_expires_at: string;
            user: components["schemas"]["UserResponse"];
        };
        /**
         * TransitionResponse
         * @description The result of either gate.
         *
         *     Both gates return the same shape on purpose: a client that has just moved
         *     a question reads old_status/new_status and needs nothing else. review_id
         *     is populated only by the review gate — promotion writes no
         *     question_reviews row, because publishing is an operational decision and
         *     not a content-quality judgement (scripts/review_question.py::
         *     promote_question). A null review_id on a promote response is that
         *     distinction, not a missing value.
         */
        TransitionResponse: {
            /**
             * Question Id
             * Format: uuid
             */
            question_id: string;
            /**
             * Old Status
             * @enum {string}
             */
            old_status: "draft" | "confirmed" | "rejected" | "live" | "superseded";
            /**
             * New Status
             * @enum {string}
             */
            new_status: "draft" | "confirmed" | "rejected" | "live" | "superseded";
            /**
             * Reviewer Id
             * Format: uuid
             */
            reviewer_id: string;
            /** Review Id */
            review_id?: string | null;
            /** Comment */
            comment?: string | null;
        };
        /**
         * UploadResponse
         * @description What POST /api/v1/upload returns.
         *
         *     UPLOADING AND EVALUATING ARE SEPARATE CALLS. An upload is a stored PDF and
         *     nothing more; POST /api/v1/evaluate is what queues work against it. That
         *     split exists because one uploaded booklet can be evaluated more than once —
         *     a different exam/student binding, a re-run after a reference answer is
         *     fixed, a re-run after ingestion is corrected.
         *
         *     This response previously also carried a `job_id`, because an upload used to
         *     enqueue exactly one job and the two ids were equal. That field is gone as of
         *     migration 015: there is no job at upload time any more, and returning a
         *     job id that named a job no worker would ever run was worse than returning
         *     nothing. The upload now has its own row (`booklet_uploads`), which is what
         *     `upload_id` identifies.
         */
        UploadResponse: {
            /**
             * Upload Id
             * Format: uuid
             * @description Identifies the stored booklet. Pass it to POST /api/v1/evaluate to queue an evaluation.
             */
            upload_id: string;
            /**
             * Filename
             * @description The client's original filename, as sent.
             */
            filename: string;
            /**
             * Size Bytes
             * @description Bytes actually received and stored.
             */
            size_bytes: number;
            /**
             * Blob Url
             * @description Stable 'bucket/key' storage reference — NEVER a presigned URL (CLAUDE_CONTEXT.md §10). Under storage_mode='dummy' this is a deterministic 'dummy-storage/...' placeholder with no object behind it.
             */
            blob_url: string;
            /**
             * Storage Mode
             * @description 'dummy' or 'minio' — which core/storage.py path stored it.
             */
            storage_mode: string;
            /**
             * Uploaded At
             * Format: date-time
             */
            uploaded_at: string;
        };
        /**
         * UploadSummary
         * @description One row of GET /api/v1/uploads.
         */
        UploadSummary: {
            /**
             * Upload Id
             * Format: uuid
             */
            upload_id: string;
            /** Filename */
            filename: string;
            /** Size Bytes */
            size_bytes: number;
            /** Blob Url */
            blob_url: string;
            /** Storage Mode */
            storage_mode: string;
            /**
             * Uploaded At
             * Format: date-time
             */
            uploaded_at: string;
            /**
             * Bound
             * @description Exam-binding state: true when at least one `answers` row was ingested from this upload, for any exam/student. See core/uploads.py::_BOUND_EXISTS.
             */
            bound: boolean;
        };
        /**
         * UserResponse
         * @description The authenticated account. `GET /api/v1/auth/me`, and echoed on login.
         *
         *     Carries `reviewer_id` deliberately and prominently: it is the id every
         *     provenance row this API writes is attributed to, and a client that shows
         *     "reviewed by" needs to be able to recognise itself in that history. It is
         *     NOT a field a client may send back — see RE-4 in api/deps/identity.py.
         */
        UserResponse: {
            /**
             * User Id
             * Format: uuid
             */
            user_id: string;
            /**
             * Reviewer Id
             * Format: uuid
             */
            reviewer_id: string;
            /** Email */
            email: string;
            /**
             * Role
             * @enum {string}
             */
            role: "teacher" | "admin" | "platform_admin";
            /**
             * College Id
             * @description The tenant this account acts within. Null if and only if the role is platform_admin (migration 017's CHECK), which is a cross-tenant account with no college of its own — not an account whose college is unknown.
             */
            college_id?: string | null;
        };
        /** ValidationError */
        ValidationError: {
            /** Location */
            loc: (string | number)[];
            /** Message */
            msg: string;
            /** Error Type */
            type: string;
            /** Input */
            input?: unknown;
            /** Context */
            ctx?: Record<string, never>;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    health_health_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
        };
    };
    login_api_v1_auth_login_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LoginRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TokenResponse"];
                };
            };
            /** @description Incorrect email or password. This is also the answer for a disabled account and for one whose college is suspended — see the module docstring. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many failed attempts. `Retry-After` says when to try again. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    refresh_api_v1_auth_refresh_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RefreshRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TokenResponse"];
                };
            };
            /** @description The refresh token is unknown, expired, or has been revoked — one answer for all three. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    logout_api_v1_auth_logout_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LogoutRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    me_api_v1_auth_me_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UserResponse"];
                };
            };
        };
    };
    list_uploads_api_v1_uploads_get: {
        parameters: {
            query?: {
                /** @description Exam-binding state: true = ingested against at least one exam/student; false = uploaded but not yet acted on. Null does not filter. See core/uploads.py. */
                bound?: boolean | null;
                /** @description Rows per page. Values above 200 are CLAMPED to 200, not rejected — see this module's docstring. `total` in the response reports the real count so a client can tell it is seeing a partial page. */
                limit?: number;
                /** @description Rows to skip, for the next page. */
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Page_UploadSummary_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    upload_booklet_api_v1_upload_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "multipart/form-data": components["schemas"]["Body_upload_booklet_api_v1_upload_post"];
            };
        };
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UploadResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description This college's request rate for this endpoint is at its configured cap (UPLOAD_RATE_LIMIT_*). `Retry-After` says how long to wait — see api/deps/quota.py. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    list_jobs_api_v1_jobs_get: {
        parameters: {
            query?: {
                /** @description Filter by evaluation_job_status. `status` is aliased from `status_filter` for the same reason api/routers/questions.py does — `status` is also the FastAPI status module imported in this file. */
                status?: ("queued" | "running" | "succeeded" | "failed") | null;
                /** @description e.g. 'booklet_ingest' or 'booklet_eval'. */
                job_type?: string | null;
                /** @description Only jobs created strictly after this timestamp — exclusive, so polling with the newest id you already have cannot see it again. */
                created_after?: string | null;
                /** @description Rows per page. Values above 200 are CLAMPED to 200, not rejected — see this module's docstring. `total` in the response reports the real count so a client can tell it is seeing a partial page. */
                limit?: number;
                /** @description Rows to skip, for the next page. */
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Page_JobResponse_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_job_api_v1_jobs__job_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobResponse"];
                };
            };
            /** @description No such job for this college. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    evaluate_api_v1_evaluate_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["EvaluateRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EvaluateResponse"];
                };
            };
            /** @description No such upload, or no such paper. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Either this college's request rate for this endpoint, or its concurrent queued+running job backlog (MAX_QUEUED_JOBS_PER_COLLEGE), is at its configured cap. `Retry-After` says how long to wait — see api/deps/quota.py. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    list_results_api_v1_results_get: {
        parameters: {
            query?: {
                exam_id?: string | null;
                student_id?: string | null;
                /** @description Filter by answer_status. */
                status?: ("pending_evaluation" | "ai_scored" | "sme_reviewed" | "finalized" | "flagged") | null;
                /** @description true = at least one region flagged for review; false = none are. Null does not filter. */
                needs_review?: boolean | null;
                /** @description Rows per page. Values above 200 are CLAMPED to 200, not rejected — see this module's docstring. `total` in the response reports the real count so a client can tell it is seeing a partial page. */
                limit?: number;
                /** @description Rows to skip, for the next page. */
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Page_ResultSummary_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_result_api_v1_results__answer_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                answer_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ResultResponse"];
                };
            };
            /** @description No such answer for this college. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_page_image_api_v1_results__answer_id__pages__page_number__image_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                answer_id: string;
                page_number: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PageImageResponse"];
                };
            };
            /** @description No such answer for this college, no region on that page, or no image stored for it — one response for all four. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description The page image is a dummy-storage reference; there is no object storage behind it. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    override_result_api_v1_results__answer_id__override_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                answer_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["OverrideRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OverrideResponse"];
                };
            };
            /** @description No such answer for this college. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    list_questions_api_v1_questions_get: {
        parameters: {
            query?: {
                /** @description Filter by lifecycle status. `draft` is the review queue. */
                status?: ("draft" | "confirmed" | "rejected" | "live" | "superseded") | null;
                style?: ("long" | "short" | "one_word" | "mcq") | null;
                source_type?: ("sentence" | "paragraph" | "diagram" | "table" | "formula" | "manual") | null;
                /** @description true = LLM-generated, false = human-authored. */
                is_ai_generated?: boolean | null;
                /** @description Only questions assigned to this generated paper. This is the closest available filter to 'questions for exam X': no question links to an exam in this schema, only to a paper, via paper_questions -> paper_sections -> generated_papers. */
                paper_id?: string | null;
                /** @description Rows per page. Values above 200 are CLAMPED to 200, not rejected — see this module's docstring. `total` in the response reports the real count so a client can tell it is seeing a partial page. */
                limit?: number;
                /** @description Rows to skip, for the next page. */
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QuestionListResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_question_api_v1_questions__question_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                question_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QuestionDetail"];
                };
            };
            /** @description No such question. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    generate_questions_api_v1_questions_generate_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GenerateQuestionsRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GenerateQuestionsResponse"];
                };
            };
            /** @description stub_llm requested outside development. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such paragraph. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The paragraph is superseded. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description This caller's request rate for this endpoint is at its configured cap (QUESTIONS_GENERATE_RATE_LIMIT_*). `Retry-After` says how long to wait — see api/deps/quota.py. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    review_question_api_v1_questions__question_id__review_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                question_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReviewRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TransitionResponse"];
                };
            };
            /** @description No such reviewer. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such question. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The question is not a draft — it was already reviewed. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    promote_question_api_v1_questions__question_id__promote_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                question_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TransitionResponse"];
                };
            };
            /** @description No such reviewer. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such question. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The question is not 'confirmed'. A draft returns this — it must be reviewed first; promotion cannot substitute for review. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    list_papers_api_v1_papers_get: {
        parameters: {
            query?: {
                status?: ("draft" | "finalized") | null;
                pattern_id?: string | null;
                /** @description Rows per page. Values above 200 are CLAMPED to 200, not rejected — see this module's docstring. `total` in the response reports the real count so a client can tell it is seeing a partial page. */
                limit?: number;
                /** @description Rows to skip, for the next page. */
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Page_PaperSummary_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    generate_paper_api_v1_papers_generate_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PaperGenerateRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaperGenerateResponse"];
                };
            };
            /** @description The caller's reviewer_id names no reviewers row. Only reachable if the account's reviewer was deleted out from under its login, which migration 017's ON DELETE RESTRICT prevents. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such paper pattern. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The pattern is retired or empty, or a mandatory slot had no matching live question. Nothing is persisted. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description This caller's request rate for this endpoint is at its configured cap (PAPERS_GENERATE_RATE_LIMIT_*). `Retry-After` says how long to wait — see api/deps/quota.py. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    list_exams_api_v1_exams_get: {
        parameters: {
            query?: {
                status?: ("draft" | "live" | "closed") | null;
                /** @description Rows per page. Values above 200 are CLAMPED to 200, not rejected — see this module's docstring. `total` in the response reports the real count so a client can tell it is seeing a partial page. */
                limit?: number;
                /** @description Rows to skip, for the next page. */
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Page_ExamSummary_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    list_students_api_v1_students_get: {
        parameters: {
            query?: {
                /** @description Only students with at least one answer on this exam. There is no direct student->exam link in this schema (§7C) — see core/students.py. */
                exam_id?: string | null;
                /** @description Rows per page. Values above 200 are CLAMPED to 200, not rejected — see this module's docstring. `total` in the response reports the real count so a client can tell it is seeing a partial page. */
                limit?: number;
                /** @description Rows to skip, for the next page. */
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Page_StudentSummary_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
}
