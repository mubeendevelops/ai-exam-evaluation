# Every direct dependency below is pinned exactly (==), generated 2026-09-06
# from `pip freeze` inside a working .venv-paddleocr (full transitive freeze
# in requirements.lock.txt, sibling file — that one is for reproducing the
# exact env byte-for-byte, this one stays the source of truth for what's a
# direct dependency and why). See CLAUDE_CONTEXT.md §10 for the incident this
# replaces (a past torch/torchvision fight under loose `>=` pins) and §7B/§7C
# for why paddlepaddle in particular cannot float.
psycopg2-binary==2.9.12

# Only needed if you run with --storage minio, OR for real (non-stub)
# diagram extraction (scripts/evaluate_diagram_answer.py fetches the scan
# from real storage — see core/storage.py::get_object_bytes). Not required
# for --storage dummy / --stub-extraction. Deliberately left commented out
# rather than uncommented (2026-09-06 decision): most local/CLI/test work
# never touches real storage, and this repo's Docker image already installs
# it unconditionally in its own layer (Dockerfile — real MinIO is part of
# that stack). Pinned here so the manual step has one unambiguous version
# rather than "whatever pip resolves today" — verified importable in
# .venv-paddleocr on 2026-09-06:
#   pip install boto3==1.43.82
# Dockerfile's separate `pip install boto3` should track this same pin.

# Only needed for --method embeddings in evaluate_answer.py /
# evaluate_pending.py, and for node-label matching in
# core/diagram_evaluator.py (Task 4). Not required for --method llm or --stub.
#
# PULLS IN torch (unpinned here — transitive, no direct import anywhere in
# this repo). This box has no GPU, and .venv-paddleocr's torch is the CPU
# wheel (torch==2.13.0+cpu / torchvision==0.28.0+cpu per requirements.lock.txt
# — no nvidia_* packages). A bare `pip install -r requirements.txt` on a
# fresh machine instead resolves plain PyPI torch, which bundles CUDA and
# drags in several GB of nvidia_* wheels (confirmed 2026-09-06: this failed
# outright on a flaky connection trying to fetch a 423 MB nvidia_cublas
# wheel). Install the CPU build FIRST if you don't need GPU support:
#   pip install torch==2.13.0+cpu torchvision==0.28.0+cpu --index-url https://download.pytorch.org/whl/cpu
# then `pip install -r requirements.txt` picks it up as already satisfied.
sentence-transformers==5.7.0

# Only needed for real (non-stub) handwritten-label extraction in
# core/diagram_extractor.py (Task 4): PaddleOCR's DB-based detection module
# finds candidate label regions (a plain-OpenCV heuristic detector was tried
# first and failed on real photos of handwriting on ruled paper — see the
# module docstring), then PaddleOCR's recognition module (PP-OCRv5,
# handwriting-capable) reads each one. Not required for --stub-extraction /
# --stub.
#
# Migrated 2026-08-28 from EasyOCR (detection) + TrOCR (recognition) — that
# combination needed five separately-pinned packages (easyocr, transformers,
# torch, torchvision, sentencepiece, protobuf) whose version compatibility
# was fragile in practice (see git history around 2026-08-27 for the
# torch/torchvision CPU-wheel mismatch this caused). paddleocr pulls its own
# compatible paddlepaddle; no manual pinning needed here. The old
# implementation was removed after the migration was verified (measured
# accuracy improvement + a real end-to-end DB/MinIO run — see
# core/diagram_extractor.py's migration-history note); it is no longer in
# this repo at all, only in git history before 2026-08-28.
paddleocr==3.7.0
# Pinned exactly, not just kept in place: the enable_mkldnn=False workaround
# in core/paddle_workarounds.py (CLAUDE_CONTEXT.md §7B/§7C) is measured
# against this specific paddlepaddle build (onednn_instruction.cc
# NotImplementedError on its default oneDNN path). A silent upgrade here is
# exactly the scenario that guard cannot be assumed to survive — re-test
# before ever bumping this pin, and see that helper's docstring for which
# three benchmark scripts to re-run if the guard is ever removed.
paddlepaddle==3.3.1

# Second engine in the multi-engine "fallback OCR" framework
# (core/ocr_fallback.py, core/ocr_engines/) — PaddleOCR is no longer the
# pipeline's only OCR option; every registered engine reads each label
# region and the best-confidence read wins per region (see
# core/ocr_fallback.py's module docstring). pytesseract is a thin wrapper
# that shells out to the `tesseract` binary, which is NOT a pip package —
# install it separately via your OS package manager, e.g.
# `sudo apt-get install -y tesseract-ocr` (Debian/Ubuntu) or
# `brew install tesseract` (macOS). Without the binary,
# core/ocr_engines/tesseract_engine.py raises a clear RuntimeError and
# core/ocr_fallback.py skips it, falling back to whichever other engines
# ARE available — not required for --stub-extraction / --stub.
pytesseract==0.3.13

# OpenCV (cv2) is imported DIRECTLY by core/diagram_shapes.py and
# core/table_extractor.py, but is deliberately NOT pinned here. It already
# arrives transitively via paddleocr, and this venv currently resolves cv2 to
# opencv-python-headless while also having opencv-contrib-python installed —
# adding a pin for one distribution on top of that would make the double
# install worse, not better. Relevant finding from the table-extractor engine
# comparison (CLAUDE_CONTEXT.md §7B): the img2table library needs
# opencv-contrib-python specifically (cv2.ximgproc.niBlackThreshold) and
# fails outright against opencv-python-headless — so do not swap the OpenCV
# distribution without re-testing the diagram pipeline, which depends on it.
#
# core/diagram_extractor.py and core/ocr_engines/ import these directly
# (PIL.Image for the fetched scan / engine crops, numpy to feed PaddleOCR's
# detector/recognizer arrays) — both currently arrive transitively via
# paddleocr/sentence-transformers, but a direct import should have a direct
# pin rather than relying on another package's dependency graph to keep
# providing it.
Pillow==12.3.0
numpy==2.2.6

# PDF page rasterization for booklet ingestion (core/booklet_ingest.py, Task 5).
# Chosen over pdf2image deliberately: pdf2image is a thin wrapper that shells
# out to poppler's `pdftoppm`, an OS binary that is NOT pip-installable and
# would add a second "install this outside the venv" setup step of exactly the
# kind core/ocr_engines/tesseract_engine.py already has to apologize for (see
# the pytesseract note above — the tesseract binary is the existing example of
# this cost). pypdfium2 bundles its own libpdfium and needs nothing installed
# outside the venv.
#
# Already present transitively (paddlex pulls it in for its own PDF input
# support), but core/booklet_ingest.py imports it DIRECTLY, and a direct
# import gets a direct pin rather than relying on another package's dependency
# graph to keep providing it — the same rationale stated for Pillow/numpy
# immediately above.
pypdfium2==5.13.0

# ──────────────────────────────────────────────
# Test tooling (tests/, pytest.ini, Makefile `make test*` targets).
# Pinned to the exact versions that installed into .venv-paddleocr on
# 2026-08-31 (`pip install pytest pytest-cov` then `pip show`), not guessed —
# characterization tests should run against a known-good pytest, not
# whatever floats in on a loose `>=`.
# ──────────────────────────────────────────────
pytest==9.1.1
pytest-cov==7.1.0

# ──────────────────────────────────────────────
# API layer (api/) — FastAPI over the same core/ library the scripts use.
# The API imports core/ directly; it never shells out to scripts/ and never
# reimplements evaluation logic (see api/__init__.py).
# ──────────────────────────────────────────────
fastapi==0.141.1
uvicorn==0.52.4
# Multipart form parsing — not used by any endpoint yet, but every planned
# ingestion endpoint (booklet PDF / scan upload) is a file upload, and FastAPI
# raises at import time on a File()/UploadFile parameter without it.
python-multipart==0.0.32
# Typed access to the SAME .env keys the CLI already uses (PG*, MINIO_*,
# GROQ_*) — see api/settings.py. Not a second config source: it copies the
# resolved values back into os.environ, because core/db.py, core/storage.py
# and core/llm.py read os.environ directly and are shared with scripts/,
# which have no FastAPI in scope.
pydantic-settings==2.15.0

# Async test support for tests/test_api/ — those tests drive the real ASGI app
# through httpx.AsyncClient rather than mocking a connection, because the
# thing under test is the request→dependency→transaction chain (does
# app.current_college_id actually get set inside the request's transaction),
# which a mocked connection would confirm without proving.
# httpx already arrives transitively via fastapi's test tooling / paddle stack;
# pinned directly here since tests import it.
httpx==0.28.1
pytest-asyncio==1.4.0

# ──────────────────────────────────────────────
# Authentication (api/deps/identity.py, api/routers/auth.py, core/users.py).
# Migration 017 owns the schema; these two libraries own the two cryptographic
# primitives the API needs and nothing else.
# ──────────────────────────────────────────────
# JWT encode/decode for the short-lived ACCESS token. [cryptography] rather
# than bare python-jose so HS256 signing/verification uses the audited
# `cryptography` backend instead of jose's pure-Python fallback, and so a
# later move to RS256 (a public key the worker can verify without holding the
# signing secret) needs no new dependency.
python-jose[cryptography]==3.5.0
# Password hashing. passlib's CryptContext is what makes the stored string
# self-describing and the scheme swappable: core/users.py hashes new passwords
# with bcrypt and still verifies the scrypt hashes written before this pass
# (see verify_password there), so no existing account is invalidated.
passlib[bcrypt]==1.7.4
# bcrypt is pinned to a known-good version deliberately, not lazily. passlib 1.7.4 reads
# `bcrypt.__about__.__version__` to detect its backend; 4.1 removed that
# attribute (passlib logs a traceback and carries on) and 5.0 removed the
# automatic >72-byte truncation passlib relies on, which turns every
# hash()/verify() into `ValueError: password cannot be longer than 72 bytes`.
# Verified in this venv on 2026-09-06: bcrypt 5.0.0 raised on a 15-character
# password; 4.3.0 works. Pinned exactly (not just <5) now that a lockfile
# exists to track the working version precisely; re-open to >=4.0,<5 if a
# reason to float within that range ever comes up. Unpin past 5 entirely
# when passlib ships a release that supports it.
bcrypt==4.3.0
